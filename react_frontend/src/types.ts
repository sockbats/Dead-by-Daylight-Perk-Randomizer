export interface killer {
    killer_id: number,
    name: string,
    title: string,
    image: string
}

export interface killer_perk {
    perk_id: number,
    name: string,
    icon: string,
    killer_title: string,
    killer_id: number
}